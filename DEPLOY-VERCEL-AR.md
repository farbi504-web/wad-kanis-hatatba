# 🚀 دليل نشر واد كنيس حطاطبة على Vercel

دليل مفصّل خطوة بخطوة، من الصفر حتى موقع يعمل على الإنترنت برابط مباشر.

**الوقت:** 30–45 دقيقة · **التكلفة:** مجاني بالكامل · **لا يحتاج خبرة سابقة**

---

## 📑 الفهرس

| # | الخطوة | المدة |
|---|--------|------|
| 0 | [قبل أن تبدأ](#الخطوة-0-قبل-أن-تبدأ) | 3 د |
| 1 | [إنشاء قاعدة البيانات على Neon](#الخطوة-1-قاعدة-البيانات-neon) | 7 د |
| 2 | [توليد المفاتيح السرية](#الخطوة-2-توليد-المفاتيح-السرية) | 3 د |
| 3 | [ربط المشروع بـ Vercel](#الخطوة-3-ربط-المشروع-بـ-vercel) | 5 د |
| 4 | [إدخال متغيرات البيئة](#الخطوة-4-متغيرات-البيئة-أهم-خطوة) | 8 د |
| 5 | [أول نشر](#الخطوة-5-أول-نشر) | 4 د |
| 6 | [تفعيل تخزين الصور](#الخطوة-6-تخزين-الصور-vercel-blob) | 4 د |
| 7 | [إنشاء جداول قاعدة البيانات](#الخطوة-7-إنشاء-جداول-قاعدة-البيانات) | 7 د |
| 8 | [تهيئة البيانات الأساسية](#الخطوة-8-تهيئة-البيانات-الأساسية) | 3 د |
| 9 | [التحقق النهائي](#الخطوة-9-التحقق-النهائي) | 5 د |

بعدها: [ربط نطاق](#-ربط-نطاق-خاص-اختياري) · [حل المشاكل](#-حل-المشاكل-الشائعة) · [التكاليف](#-الحدود-المجانية-والتكاليف)

---

## الخطوة 0: قبل أن تبدأ

### ما تحتاجه

- **حساب GitHub** — لديك واحد بالفعل (`farbi504-web`)
- **بريد إلكتروني** — للتسجيل في Neon و Vercel
- **متصفح** — معظم العمل من المتصفح

### هل تحتاج تثبيت برامج؟

نعم، للخطوة 7 فقط (إنشاء الجداول). تحتاج:

- **Node.js 20 أو أحدث** — [nodejs.org](https://nodejs.org) (اختر LTS)
- **Git** — [git-scm.com](https://git-scm.com)

للتأكد أنهما مثبتان، افتح Terminal (أو Git Bash على ويندوز) واكتب:

```bash
node --version    # يجب أن يظهر v20 أو أعلى
git --version     # يجب أن يظهر أي رقم
```

> 💡 **بديل بلا تثبيت:** إن لم ترغب في تثبيت شيء، تستطيع تنفيذ الخطوة 7
> من متصفحك عبر **GitHub Codespaces** (مجاني 60 ساعة شهرياً). الشرح في
> [الخطوة 7 — الطريقة ب](#الطريقة-ب-من-المتصفح-بلا-تثبيت).

### ⚠️ ملاحظة مهمة عن الفرع

الكود الكامل موجود على فرع **`arena/019fa039-wad-kanis-hatatba`** وليس على `main`.
انتبه لهذا في الخطوتين 3 و7.

---

## الخطوة 1: قاعدة البيانات (Neon)

قاعدة البيانات هي المكان الذي تُحفظ فيه الإعلانات والمستخدمون. نستخدم **Neon** لأنه مجاني ويعمل بامتياز مع Vercel.

### 1.1 إنشاء الحساب

1. افتح [neon.tech](https://neon.tech)
2. اضغط **Sign Up**
3. اختر **Continue with GitHub** (الأسهل)
4. وافق على الأذونات

### 1.2 إنشاء المشروع

بعد الدخول سيطلب منك إنشاء مشروع:

| الحقل | القيمة |
|-------|--------|
| **Project name** | `wad-kanis` |
| **Postgres version** | اتركه الافتراضي (17 أو أحدث) |
| **Region** | **Europe (Frankfurt)** — الأقرب للجزائر |

> 🌍 **لماذا فرانكفورت؟** لأنها أقرب مركز بيانات للجزائر، فيكون الموقع أسرع
> لزوارك. لا تختر مناطق أمريكية.

اضغط **Create Project**.

### 1.3 نسخ رابط الاتصال

بعد الإنشاء تظهر نافذة **Connect to your database**. إن أُغلقت، اضغط زر **Connect** في لوحة المشروع.

سترى خياراً باسم **Connection pooling** أو قائمة بنوعين من الروابط. **هذا مهم جداً:**

| النوع | الشكل | متى نستخدمه |
|------|-------|-------------|
| **Pooled** ✅ | يحتوي `-pooler` في العنوان | **للموقع** (الخطوة 4) |
| **Direct** | بلا `-pooler` | **لإنشاء الجداول** (الخطوة 7) |

**انسخ الاثنين واحفظهما مؤقتاً** في ملف نصي على جهازك.

مثال على الشكل:

```
# Pooled (للموقع)
postgresql://neondb_owner:AbC123@ep-cool-name-a1b2-pooler.eu-central-1.aws.neon.tech/neondb?sslmode=require

# Direct (للجداول)
postgresql://neondb_owner:AbC123@ep-cool-name-a1b2.eu-central-1.aws.neon.tech/neondb?sslmode=require
```

> 🔍 **الفرق بينهما:** الرابط الـ pooled يدير آلاف الاتصالات المتزامنة — وهو ما
> يحتاجه موقع يعمل على Vercel. الرابط المباشر أنسب لأدوات إنشاء الجداول والنسخ
> الاحتياطي.

> 🔐 **تحذير:** هذا الرابط يحتوي كلمة مرور قاعدة بياناتك. لا تضعه في ملفات
> المشروع، ولا ترفعه إلى GitHub، ولا تشاركه مع أحد. إن تسرّب، اذهب إلى
> Neon ← Roles ← **Reset password** فوراً.

---

## الخطوة 2: توليد المفاتيح السرية

الموقع يحتاج 4 مفاتيح سرية لتشفير الجلسات وأرقام الهواتف.

### الطريقة أ: السكربت الجاهز (موصى بها)

إن كان المشروع على جهازك:

```bash
bash scripts/generate-secrets.sh
```

### الطريقة ب: يدوياً

على Linux / macOS / Git Bash:

```bash
echo "AUTH_SECRET=$(openssl rand -base64 48)"
echo "ENCRYPTION_KEY=$(openssl rand -hex 32)"
echo "ENCRYPTION_SALT=$(openssl rand -hex 16)"
echo "SEED_SECRET=$(openssl rand -base64 48)"
```

### الطريقة ج: بلا أدوات (من المتصفح)

افتح [generate-secret.vercel.app/64](https://generate-secret.vercel.app/64) وحدّث الصفحة 4 مرات، وانسخ قيمة مختلفة لكل مفتاح.

---

**احفظ الناتج في ملف نصي مؤقت**، سيكون شبيهاً بهذا:

```
AUTH_SECRET=k3Jd9vXm2pQr7sT1uY4wZ6aB8cE0fG5hI2jK4lM6nO8pQ0rS2tU4vW6xY8z
ENCRYPTION_KEY=a1b2c3d4e5f6789012345678901234567890abcdef1234567890abcdef123456
ENCRYPTION_SALT=9f8e7d6c5b4a39281706f5e4d3c2b1a0
SEED_SECRET=Zx9Yw8Vu7Ts6Rq5Po4Nm3Lk2Ji1Hg0Fe9Dc8Ba7...
```

### 🔴 تحذيرات لا تتجاهلها

> **`ENCRYPTION_KEY` يشفّر أرقام هواتف المستخدمين.**
> إن غيّرته بعد أن يسجّل مستخدمون، **لن تُقرأ أرقامهم القديمة أبداً**.
> ولّده مرة واحدة، واحتفظ بنسخة في مكان آمن (مدير كلمات مرور مثلاً).

> **لا تستخدم قيماً بسيطة** مثل `123456` أو `mysecret`.
> الموقع يعمل بها لكنه يصبح مخترقاً بسهولة.

> **الكود يحتوي قيماً احتياطية ضعيفة** تعمل إن نسيت ضبط المفاتيح —
> ولن يظهر لك أي خطأ. لذلك تأكد من ضبط الأربعة كلها في الخطوة 4.

---

## الخطوة 3: ربط المشروع بـ Vercel

### 3.1 إنشاء الحساب

1. افتح [vercel.com](https://vercel.com)
2. **Sign Up** ← **Continue with GitHub**
3. وافق على الأذونات
4. اختر خطة **Hobby** (المجانية)

### 3.2 استيراد المشروع

1. من لوحة التحكم اضغط **Add New...** ← **Project**
2. ستظهر قائمة مستودعاتك على GitHub
3. ابحث عن `wad-kanis-hatatba` واضغط **Import**

> إن لم يظهر المستودع: اضغط **Adjust GitHub App Permissions** ثم امنح Vercel
> صلاحية الوصول إليه.

### 3.3 إعدادات المشروع

ستظهر صفحة **Configure Project**:

| الإعداد | القيمة |
|---------|--------|
| **Project Name** | `wad-kanis-hatatba` (أو ما تشاء) |
| **Framework Preset** | **Next.js** — يُكتشف تلقائياً ✅ |
| **Root Directory** | `./` (اتركه) |
| **Build Command** | اتركه فارغاً (افتراضي) |
| **Install Command** | اتركه فارغاً (افتراضي) |

### 3.4 ⚠️ اختيار الفرع الصحيح

هذه أهم نقطة في هذه الخطوة.

ابحث عن قائمة **Branch** (قد تكون تحت اسم المستودع أو في قسم Git):

**اختر: `arena/019fa039-wad-kanis-hatatba`**

> إن لم تجد خيار الفرع في هذه الشاشة، لا تقلق — أكمل النشر، ثم غيّره لاحقاً من:
> **Settings** ← **Git** ← **Production Branch** ← اختر الفرع ← **Save**،
> ثم أعد النشر.

### 🛑 لا تضغط Deploy الآن

انتقل مباشرة للخطوة 4 وأدخل المتغيرات أولاً. إن نشرت قبلها **سيفشل النشر** برسالة:

```
Error: DATABASE_URL is required
```

---

## الخطوة 4: متغيرات البيئة (أهم خطوة)

في نفس صفحة الإعداد، افتح قسم **Environment Variables**.

### 4.1 المتغيرات الإلزامية

أضف هذه **التسعة** واحداً واحداً (اكتب الاسم، الصق القيمة، ثم **Add**):

| # | الاسم | القيمة | من أين |
|---|------|--------|--------|
| 1 | `DATABASE_URL` | رابط Neon **الـ pooled** | الخطوة 1.3 |
| 2 | `DATABASE_SSL` | `true` | اكتبها يدوياً |
| 3 | `AUTH_SECRET` | المفتاح المولّد | الخطوة 2 |
| 4 | `ENCRYPTION_KEY` | المفتاح المولّد | الخطوة 2 |
| 5 | `ENCRYPTION_SALT` | المفتاح المولّد | الخطوة 2 |
| 6 | `SEED_SECRET` | المفتاح المولّد | الخطوة 2 |
| 7 | `FORCE_SECURE_COOKIE` | `1` | اكتبها يدوياً |
| 8 | `ADMIN_EMAIL` | `admin@wad-kanis.dz` | أو بريدك |
| 9 | `ADMIN_PASSWORD` | كلمة مرور قوية (**12 حرفاً فأكثر**) | اخترها أنت |

### 🔴 `DATABASE_SSL=true` ضرورية — لا تنساها

قد تظن أن `?sslmode=require` في رابط Neon يكفي. **لا يكفي.**

المكتبة المستخدمة في المشروع (`pg`) **لا تقرأ `sslmode` من الرابط إطلاقاً**.
بدون `DATABASE_SSL=true` سيفشل الاتصال بقاعدة البيانات، وسترى في السجلات:

```
Error: The server does not support SSL connections
```

### 4.2 نطاق التفعيل

لكل متغير، تأكد أن الخانات الثلاث مُحدَّدة:

- ☑️ **Production**
- ☑️ **Preview**
- ☑️ **Development**

### 4.3 أخطاء شائعة عند اللصق

| الخطأ | النتيجة |
|-------|---------|
| مسافة في بداية/نهاية القيمة | فشل الاتصال أو الدخول |
| علامات اقتباس `"..."` حول القيمة | تُقرأ كجزء من القيمة → خطأ |
| نسخ ناقص للرابط الطويل | فشل الاتصال |
| استخدام الرابط **direct** بدل pooled | يعمل، لكن قد ينفد الاتصال تحت الضغط |

> ✅ **تحقق:** رابط `DATABASE_URL` يجب أن ينتهي باسم قاعدة البيانات (مثل `/neondb?sslmode=require`)
> وأن يحتوي `-pooler` في العنوان.

---

## الخطوة 5: أول نشر

اضغط **Deploy** وانتظر 2–4 دقائق.

### ماذا يحدث الآن؟

```
Cloning repository...        ← يسحب الكود من GitHub
Installing dependencies...   ← يثبّت 519 حزمة
Building...                  ← يبني الموقع (~30 ثانية)
Deploying...                 ← ينشره عالمياً
```

### ✅ النتيجة

ستظهر شاشة تهنئة مع رابط مثل:

```
https://wad-kanis-hatatba.vercel.app
```

**افتحه.** سيفتح الموقع لكن **الصفحة الرئيسية ستكون شبه فارغة** — لا فئات ولا مدن.

> هذا **طبيعي تماماً**. قاعدة البيانات موجودة لكنها فارغة بلا جداول. الخطوتان
> 7 و8 تحلّان هذا.

### ❌ إن فشل النشر

اضغط على النشر الفاشل واقرأ **Build Logs**. الأخطاء الشائعة:

| الرسالة | السبب | الحل |
|---------|-------|------|
| `DATABASE_URL is required` | نسيت المتغير | أضفه ثم **Redeploy** |
| `Failed to fetch Cairo from Google Fonts` | عطل مؤقت في الشبكة | أعد النشر |
| `Module not found` | مشكلة في الحزم | تأكد من الفرع الصحيح |

**لإعادة النشر:** Deployments ← آخر نشر ← زر **⋯** ← **Redeploy**

---

## الخطوة 6: تخزين الصور (Vercel Blob)

### 🔴 لماذا هذه الخطوة إلزامية؟

نظام ملفات Vercel **مؤقت**. أي صورة يرفعها المستخدم تُحذف عند إعادة تشغيل
الخادم (يحدث تلقائياً عدة مرات يومياً).

**بدون هذه الخطوة: كل صور الإعلانات ستختفي.**

### الخطوات

1. من لوحة مشروعك اضغط تبويب **Storage**
2. **Create Database** (أو **Connect Store**)
3. اختر **Blob**
4. الاسم: `wad-kanis-images`
5. **Create**
6. اضغط **Connect Project** واختر مشروعك

Vercel يضيف متغير `BLOB_READ_WRITE_TOKEN` تلقائياً. الكود يكتشفه ويحوّل التخزين للسحابة من تلقاء نفسه.

### 7. أعد النشر

المتغير الجديد لا يُفعَّل إلا بنشر جديد:

**Deployments** ← آخر نشر ← **⋯** ← **Redeploy**

> 💡 الكود مكتوب ليعمل في الحالتين: بوجود التوكن يستخدم Blob، وبدونه يستخدم
> القرص المحلي (مفيد للتطوير أو خادم VPS بقرص دائم).

---

## الخطوة 7: إنشاء جداول قاعدة البيانات

قاعدة البيانات فارغة. هذه الخطوة تُنشئ **24 جدولاً** (المستخدمون، الإعلانات، الرسائل، إلخ).

### الطريقة أ: من جهازك

```bash
# 1. نزّل المشروع
git clone https://github.com/farbi504-web/wad-kanis-hatatba.git
cd wad-kanis-hatatba

# 2. انتقل للفرع الصحيح (مهم!)
git checkout arena/019fa039-wad-kanis-hatatba

# 3. ثبّت الحزم (2-3 دقائق)
npm ci

# 4. اضبط الرابط — استخدم الرابط الـ direct (بلا -pooler)
export DATABASE_URL="postgresql://...الرابط-المباشر-الكامل..."
export DATABASE_SSL=true

# 5. أنشئ الجداول
npx drizzle-kit push
```

**على ويندوز (PowerShell)** استبدل خطوة 4 بـ:

```powershell
$env:DATABASE_URL="postgresql://...الرابط-المباشر..."
$env:DATABASE_SSL="true"
```

عند السؤال عن التأكيد اكتب `y` واضغط Enter.

### ✅ النتيجة المتوقعة

```
[✓] Pulling schema from database...
[✓] Changes applied
```

### الطريقة ب: من المتصفح (بلا تثبيت)

إن لم ترغب في تثبيت Node.js:

1. افتح مستودعك على GitHub
2. اضغط زر **Code** الأخضر ← تبويب **Codespaces** ← **Create codespace**
3. انتظر تحميل بيئة العمل (دقيقة)
4. في الطرفية السفلية نفّذ نفس أوامر الطريقة أ (بدءاً من `git checkout`)

> Codespaces مجاني حتى 60 ساعة شهرياً، ويأتي بـ Node.js و Git جاهزين.

### 🔍 لماذا الرابط المباشر (direct) هنا؟

وثائق Neon الرسمية تذكر **Drizzle Kit بالاسم** ضمن الأدوات التي تحتاج اتصالاً
مباشراً، لأن عمليات تعديل بنية الجداول (DDL) لا تعمل بشكل موثوق عبر PgBouncer
المستخدم في الرابط الـ pooled.

باختصار:

| الاستخدام | الرابط |
|-----------|--------|
| الموقع على Vercel (الخطوة 4) | **pooled** — يتحمّل آلاف الاتصالات |
| إنشاء/تعديل الجداول (هذه الخطوة) | **direct** — مستقر للعمليات الطويلة |

---

## الخطوة 8: تهيئة البيانات الأساسية

هذه الخطوة تملأ الموقع بـ **8 مدن** و**12 فئة** وتُنشئ **حساب المدير**.

### التنفيذ

استبدل القيمتين برابط موقعك و `SEED_SECRET` الذي ولّدته:

```bash
curl -X POST https://your-app.vercel.app/api/seed \
  -H "x-seed-secret: قيمة_SEED_SECRET_هنا"
```

**على ويندوز (PowerShell):**

```powershell
Invoke-RestMethod -Method Post -Uri "https://your-app.vercel.app/api/seed" `
  -Headers @{"x-seed-secret"="قيمة_SEED_SECRET_هنا"}
```

### ✅ النتيجة المتوقعة

```json
{
  "data": {
    "ok": true,
    "message": "تم تهيئة البيانات الأساسية",
    "admin": "تم إنشاء حساب المدير: admin@wad-kanis.dz"
  }
}
```

### ما الذي أُنشئ؟

| النوع | العدد | أمثلة |
|------|-------|-------|
| **المدن** | 8 | حطاطبة، تيبازة، القليعة، حجوط، شرشال، بوهارون، فوكة، سيدي أعمر |
| **الفئات** | 12 | سيارات، عقارات، هواتف، إلكترونيات، أجهزة منزلية، أزياء، خدمات، وظائف، حيوانات، كتب، ألعاب، رياضة |
| **حساب المدير** | 1 | بالبريد وكلمة المرور من الخطوة 4 |

### 🔐 عن حساب المدير

بيانات الدخول هي `ADMIN_EMAIL` و `ADMIN_PASSWORD` اللذان ضبطتهما في الخطوة 4.

> **لا توجد كلمة مرور مكتوبة في الكود** — لأن المستودع عام وأي شخص كان
> سيقرأها. إن نسيت ضبط `ADMIN_PASSWORD` سترفض التهيئة إنشاء الحساب وتخبرك
> بذلك صراحةً.

> ✅ **التهيئة آمنة التكرار:** تستطيع تشغيلها أكثر من مرة دون أن تُكرَّر المدن
> أو الفئات. إن فشلت في منتصفها، أعد تشغيلها وستُكمل ما نقص.

---

## الخطوة 9: التحقق النهائي

### 9.1 فحص سريع

```bash
curl https://your-app.vercel.app/api/health
```

المتوقع:

```json
{"ok":true,"result":[{"ok":1}]}
```

إن ظهر `"ok":false` أو خطأ → راجع [حل المشاكل](#-حل-المشاكل-الشائعة).

### 9.2 قائمة تحقق من المتصفح

افتح موقعك وتأكد من كل بند:

- [ ] الصفحة الرئيسية تعرض **12 فئة** و**8 مدن**
- [ ] الكتابة العربية تظهر من اليمين لليسار بخط واضح
- [ ] **إنشاء حساب جديد** يعمل
- [ ] **تسجيل الدخول** بالحساب الجديد يعمل
- [ ] **نشر إعلان** مع صورة يعمل
- [ ] الصورة تظهر بعد الرفع ✅ (تأكيد عمل Blob)
- [ ] **دخول المدير** بـ `ADMIN_EMAIL` يعمل
- [ ] **لوحة التحكم** `/admin` تفتح للمدير فقط
- [ ] فتح `/admin` بحساب عادي يُرفض

### 9.3 اختبار بقاء الصور (مهم)

بعد رفع صورة، انتظر **10 دقائق** ثم حدّث الصفحة.

- الصورة باقية → ✅ Blob يعمل
- الصورة اختفت → ❌ راجع الخطوة 6 وأعد النشر

---

## ⚙️ الإعلانات لا تظهر مباشرة — هذا مقصود

الإعلانات الجديدة تُنشأ بحالة **"قيد المراجعة"** ولا تظهر للعامة حتى توافق عليها.

هذه حماية من الإعلانات المسيئة أو الاحتيالية.

### للموافقة على إعلان

**لوحة التحكم** ← **الإعلانات** ← اختر الإعلان ← **موافقة**

### لجعل النشر فورياً بلا مراجعة

**لوحة التحكم** ← **الإعدادات** ← فعّل `auto_approve_listings`

> 💡 نصيحة: أبقِ المراجعة مفعّلة في البداية حتى تفهم نمط الإعلانات الواردة.

---

## 🌐 ربط نطاق خاص (اختياري)

### إن كان لديك نطاق

1. **Settings** ← **Domains**
2. اكتب نطاقك (مثل `wad-kanis.dz`) ← **Add**
3. Vercel يعرض سجلات DNS المطلوبة:

| النوع | الاسم | القيمة |
|------|------|--------|
| `A` | `@` | `76.76.21.21` |
| `CNAME` | `www` | `cname.vercel-dns.com` |

4. أضفها عند مزوّد النطاق
5. انتظر 10 دقائق إلى 48 ساعة (عادة أقل من ساعة)
6. شهادة SSL تُصدر تلقائياً 🔒

### عن نطاقات `.dz`

تُدار عبر [NIC.DZ](https://www.nic.dz) وتتطلب **سجلاً تجارياً جزائرياً**.

بدائل أسرع للبداية: `.com` أو `.net` من Namecheap أو Cloudflare (~10$ سنوياً)،
أو ابقَ على رابط `.vercel.app` المجاني — يعمل بكفاءة تامة.

---

## 🔧 حل المشاكل الشائعة

### مشاكل النشر

| الرسالة | السبب | الحل |
|---------|-------|------|
| `DATABASE_URL is required` | المتغير غير مضاف، أو أُضيف بعد النشر | أضفه ثم **Redeploy** |
| `The server does not support SSL` | نسيت `DATABASE_SSL=true` | أضفها ثم أعد النشر |
| `Failed to fetch Cairo from Google Fonts` | عطل شبكة مؤقت | أعد النشر |
| النشر ينجح لكن الموقع 500 | خطأ وقت التشغيل | افتح **Logs** في لوحة Vercel |

### مشاكل قاعدة البيانات

| الرسالة | السبب | الحل |
|---------|-------|------|
| `relation "users" does not exist` | لم تُنشأ الجداول | الخطوة 7 |
| `password authentication failed` | الرابط ناقص أو خاطئ | انسخه من Neon مجدداً |
| `too many connections` | استخدمت الرابط المباشر للموقع | استخدم رابط **pooled** |
| أول طلب بطيء (3 ثوانٍ) | Neon المجاني ينام بعد 5 دقائق | طبيعي — لا يؤثر عملياً |

### مشاكل التهيئة

| الرسالة | السبب | الحل |
|---------|-------|------|
| `401 Unauthorized` | `SEED_SECRET` لا يطابق المحفوظ | تحقق من نسخه كاملاً بلا مسافات |
| `ADMIN_PASSWORD غير مضبوط` | المتغير مفقود | أضفه، أعد النشر، أعد التهيئة |
| `12 حرفاً على الأقل` | كلمة المرور قصيرة | اختر أطول |

### مشاكل الاستخدام

| المشكلة | الحل |
|---------|------|
| الصفحة الرئيسية فارغة | لم تُشغّل التهيئة → الخطوة 8 |
| الصور تختفي بعد ساعات | Blob غير مربوط → الخطوة 6 + إعادة نشر |
| الإعلان لا يظهر للعامة | قيد المراجعة → وافق عليه من لوحة التحكم |
| لا أستطيع الدخول كمدير | تحقق من `ADMIN_EMAIL` و `ADMIN_PASSWORD` في Vercel |

### 📋 كيف تقرأ سجل الأخطاء؟

**لوحة Vercel** ← مشروعك ← تبويب **Logs**

يعرض الأخطاء لحظياً. عند حدوث مشكلة، افتحه وجرّب العملية الفاشلة، ثم اقرأ آخر سطر أحمر.

---

## 💰 الحدود المجانية والتكاليف

| الخدمة | الحد المجاني | متى تحتاج ترقية |
|--------|--------------|------------------|
| **Vercel Hobby** | 100 جيجا نقل/شهر | ~50 ألف زيارة شهرياً |
| **Neon Free** | 0.5 جيجا تخزين | ~100 ألف إعلان |
| **Vercel Blob** | 1 جيجا تخزين | ~3000 صورة مضغوطة |

**للبداية: مجاني تماماً.** المنصة تضغط كل صورة مرفوعة تلقائياً (جودة 85%) وتحدّ أبعادها بـ1920 بكسل، فتوفّر مساحة كبيرة.

### إن كبر الموقع

- Vercel Pro: **20$/شهر**
- Neon Scale: **19$/شهر**

> 🌙 **سبات Neon:** القاعدة المجانية تنام بعد 5 دقائق خمول، فيتأخر أول طلب
> 2–3 ثوانٍ ثم تعود السرعة طبيعية. لا يؤثر على تجربة زوارك عملياً.

---

## 🔒 قائمة الأمان بعد النشر

- [ ] **فعّل المصادقة الثنائية (2FA)** لحساب المدير — مدعومة في المنصة
- [ ] **احفظ نسخة من `ENCRYPTION_KEY`** في مدير كلمات مرور — فقدانه = فقدان أرقام الهواتف المشفّرة
- [ ] **لا تشارك** `SEED_SECRET` ولا `ADMIN_PASSWORD` مع أحد
- [ ] **راجع سجل النشاط** في لوحة التحكم دورياً
- [ ] **فعّل النسخ الاحتياطي** في Neon (متاح في الخطة المجانية: 7 أيام)

### ما هو مؤمَّن أصلاً في المنصة؟

تحققتُ من هذه عملياً على قاعدة بيانات حقيقية:

- ✅ كلمات المرور مُجزّأة بـ bcrypt
- ✅ **أرقام الهواتف مشفّرة** بـ AES-256-GCM في قاعدة البيانات
- ✅ الجلسات بـ JWT مع كوكيز آمنة (HttpOnly + Secure)
- ✅ حماية من هجمات القوة الغاشمة (10 محاولات دخول كل 15 دقيقة)
- ✅ واجهات المدير ترفض المستخدم العادي (403)
- ✅ رفع الصور يفحص المحتوى الفعلي لا الامتداد فقط
- ✅ رأسيات أمان كاملة (HSTS، X-Frame-Options، إلخ)

---

## 🔄 تحديث الموقع لاحقاً

بعد النشر الأول، أي تعديل تدفعه إلى الفرع يُنشر تلقائياً:

```bash
git add .
git commit -m "وصف التعديل"
git push origin arena/019fa039-wad-kanis-hatatba
```

Vercel يبني وينشر خلال دقيقتين.

> ⚠️ إن عدّلت `src/db/schema.ts` (بنية قاعدة البيانات)، شغّل `npx drizzle-kit push`
> مجدداً بعد الدفع.

---

## 📞 ملخص سريع للمراجعة

```
1. Neon  → أنشئ مشروعاً في فرانكفورت → انسخ الرابطين (pooled + direct)
2. ولّد  → 4 مفاتيح سرية
3. Vercel → استورد المستودع → اختر الفرع arena/019fa039-...
4. أضف   → 9 متغيرات (لا تنسَ DATABASE_SSL=true)
5. Deploy → انتظر 3 دقائق → احصل على الرابط
6. Storage → أنشئ Blob → اربطه → أعد النشر
7. محلياً → npx drizzle-kit push (بالرابط direct)
8. curl   → /api/seed مع SEED_SECRET
9. تحقق  → /api/health + جرّب الموقع
```

**بالتوفيق! 🎉**
