# 🚀 دليل نشر واد كنيس حطاطبة على Vercel

دليل عملي خطوة بخطوة للحصول على رابط مباشر للموقع.

**الوقت المتوقع:** 20–30 دقيقة · **التكلفة:** مجاني بالكامل للبداية

---

## نظرة عامة على الخطوات

| # | الخطوة | المدة |
|---|--------|------|
| 1 | إنشاء قاعدة بيانات على Neon | 5 د |
| 2 | توليد المفاتيح السرية | 2 د |
| 3 | ربط المشروع بـ Vercel | 5 د |
| 4 | إدخال متغيرات البيئة | 5 د |
| 5 | تفعيل تخزين الصور (Blob) | 3 د |
| 6 | تطبيق مخطط قاعدة البيانات | 5 د |
| 7 | تهيئة البيانات الأساسية | 2 د |

---

## الخطوة 1: قاعدة البيانات (Neon)

1. ادخل [neon.tech](https://neon.tech) وسجّل بحساب GitHub.
2. اضغط **Create Project**:
   - الاسم: `wad-kanis`
   - المنطقة: **Europe (Frankfurt)** — الأقرب للجزائر
3. بعد الإنشاء انسخ **Connection String**، ويكون بهذا الشكل:

```
postgresql://user:password@ep-xxx.eu-central-1.aws.neon.tech/neondb?sslmode=require
```

> ⚠️ احتفظ به مؤقتاً في مكان آمن — ستحتاجه في الخطوة 4.
> لا تضعه أبداً داخل ملفات المشروع أو ترفعه إلى GitHub.

---

## الخطوة 2: توليد المفاتيح السرية

شغّل هذا الأمر على جهازك (يحتاج `openssl`، متوفر على Linux/Mac؛ وعلى Windows استخدم Git Bash):

```bash
echo "AUTH_SECRET=$(openssl rand -base64 48)"
echo "ENCRYPTION_KEY=$(openssl rand -hex 32)"
echo "ENCRYPTION_SALT=$(openssl rand -hex 16)"
echo "SEED_SECRET=$(openssl rand -base64 48)"
```

أو استخدم السكربت الجاهز في المشروع:

```bash
bash scripts/generate-secrets.sh
```

انسخ الناتج كاملاً — ستلصقه في الخطوة 4.

> 🔐 **مهم:** `ENCRYPTION_KEY` يشفّر أرقام هواتف المستخدمين.
> إذا غيّرته بعد أن يسجّل مستخدمون، لن تُقرأ أرقامهم القديمة. ولّده مرة واحدة واحتفظ به.

---

## الخطوة 3: ربط المشروع بـ Vercel

1. ادخل [vercel.com](https://vercel.com) وسجّل بحساب GitHub.
2. **Add New** ← **Project**.
3. اختر مستودع `wad-kanis-hatatba`.
4. في **Configure Project**:
   - Framework Preset: **Next.js** (يُكتشف تلقائياً)
   - Root Directory: `./`
   - **⚠️ في خانة Branch اختر `arena/019fa039-wad-kanis-hatatba`**
     (الكود الكامل موجود على هذا الفرع، وليس على `main`)

> 💡 لتجعله الفرع الافتراضي لاحقاً: ادمج الفرع في `main` من GitHub، أو
> غيّره من Vercel: Settings ← Git ← Production Branch.

**لا تضغط Deploy بعد** — أدخل المتغيرات أولاً (الخطوة 4)، وإلا فشل النشر.

---

## الخطوة 4: متغيرات البيئة

في نفس الصفحة افتح **Environment Variables** وأضف:

| المفتاح | القيمة |
|---------|--------|
| `DATABASE_URL` | رابط Neon من الخطوة 1 |
| `AUTH_SECRET` | من الخطوة 2 |
| `ENCRYPTION_KEY` | من الخطوة 2 |
| `ENCRYPTION_SALT` | من الخطوة 2 |
| `SEED_SECRET` | من الخطوة 2 |
| `FORCE_SECURE_COOKIE` | `1` |
| `NODE_ENV` | `production` |
| `ADMIN_EMAIL` | بريد المدير، مثل `admin@wad-kanis.dz` |
| `ADMIN_PASSWORD` | **كلمة مرور قوية من اختيارك** (12 حرفاً فأكثر) |

تأكد أن كل متغير مفعّل على البيئات الثلاث: **Production / Preview / Development**.

الآن اضغط **Deploy** وانتظر 2–3 دقائق.

✅ ستحصل على رابط مثل: `https://wad-kanis-hatatba.vercel.app`

> الموقع سيفتح، لكن قاعدة البيانات ما تزال فارغة — أكمل الخطوات التالية.

---

## الخطوة 5: تخزين الصور (Vercel Blob)

**هذه الخطوة إلزامية.** نظام ملفات Vercel مؤقت، فبدون Blob تختفي كل الصور المرفوعة عند أي إعادة تشغيل.

1. من لوحة المشروع: **Storage** ← **Create Database** ← **Blob**.
2. الاسم: `wad-kanis-images` ← **Create**.
3. اربطه بالمشروع: **Connect Project**.

Vercel يضيف `BLOB_READ_WRITE_TOKEN` تلقائياً. الكود يكتشفه ويحوّل التخزين للسحابة من تلقاء نفسه.

4. أعد النشر ليأخذ المتغير الجديد: **Deployments** ← آخر نشر ← **⋯** ← **Redeploy**.

> بدون هذا التوكن يعود الكود للقرص المحلي تلقائياً — مفيد للتطوير أو VPS، لكنه لا يصلح على Vercel.

---

## الخطوة 6: إنشاء جداول قاعدة البيانات

قاعدة البيانات فارغة الآن. من جهازك:

```bash
git clone https://github.com/farbi504-web/wad-kanis-hatatba.git
cd wad-kanis-hatatba
git checkout arena/019fa039-wad-kanis-hatatba
npm ci

# ضع رابط Neon هنا
export DATABASE_URL="postgresql://...رابط-Neon-الكامل..."
npx drizzle-kit push
```

عند سؤاله عن التأكيد اكتب `y`. سيُنشئ **24 جدولاً**.

---

## الخطوة 7: تهيئة البيانات الأساسية

```bash
curl -X POST https://your-app.vercel.app/api/seed \
  -H "x-seed-secret: قيمة_SEED_SECRET_من_الخطوة_2"
```

يُنشئ 8 مدن + 12 فئة + حساب مدير.

**بيانات دخول المدير** هي `ADMIN_EMAIL` و `ADMIN_PASSWORD` اللذان ضبطتهما في الخطوة 4.

> 🔐 كلمة مرور المدير تُقرأ من متغيرات البيئة ولا توجد قيمة مكتوبة في الكود،
> لأن المستودع عام. إن نسيت ضبط `ADMIN_PASSWORD` سترفض التهيئة إنشاء الحساب
> وتخبرك بذلك — أضف المتغير، أعد النشر، ثم شغّل التهيئة مجدداً.

التهيئة آمنة التكرار: يمكنك تشغيلها أكثر من مرة دون تكرار المدن أو الفئات.

---

## ✅ التحقق النهائي

```bash
curl https://your-app.vercel.app/api/health
# المتوقع: {"ok":true,...}
```

ثم جرّب من المتصفح:

- [ ] الصفحة الرئيسية تعرض 12 فئة و8 مدن
- [ ] إنشاء حساب جديد يعمل
- [ ] نشر إعلان مع صورة يعمل
- [ ] الصورة تظهر بعد دقيقة (تأكيد عمل Blob)
- [ ] دخول المدير ولوحة التحكم تعمل

---

## ⚙️ ملاحظة مهمة: الإعلانات لا تظهر مباشرة

الإعلانات الجديدة تُنشأ بحالة **قيد المراجعة** ولا تظهر للعامة حتى يوافق عليها المدير — هذا سلوك مقصود لمنع الإعلانات المسيئة.

**للموافقة:** لوحة التحكم ← الإعلانات ← موافقة.

**لتعطيل المراجعة** وجعل النشر فورياً: لوحة التحكم ← الإعدادات ← فعّل `auto_approve_listings`.

---

## 🌐 ربط نطاق خاص (اختياري)

1. **Settings** ← **Domains** ← أضف نطاقك (مثل `wad-kanis.dz`).
2. أضف سجلات DNS كما يعرضها Vercel عند مزوّد النطاق.
3. شهادة SSL تُصدر تلقائياً خلال دقائق.

> نطاقات `.dz` تُدار عبر [NIC.DZ](https://www.nic.dz) وتتطلب سجلاً تجارياً جزائرياً.
> للبداية السريعة يكفي رابط `.vercel.app` المجاني.

---

## 🔧 حل المشاكل الشائعة

| المشكلة | الحل |
|---------|------|
| `DATABASE_URL is required` | المتغير غير مضاف أو النشر تم قبل إضافته → أعد النشر |
| الصور تختفي بعد ساعات | Blob غير مربوط → الخطوة 5، ثم أعد النشر |
| `relation "users" does not exist` | لم تُطبّق الجداول → الخطوة 6 |
| `ADMIN_PASSWORD غير مضبوط` | أضف المتغير في Vercel، أعد النشر، ثم أعد التهيئة |
| فشل التهيئة بـ 401 | `SEED_SECRET` المُرسل لا يطابق المحفوظ في Vercel |
| الصفحة الرئيسية فارغة | لم تُشغّل التهيئة → الخطوة 7 |
| الإعلان لا يظهر | قيد المراجعة → وافق عليه من لوحة التحكم |

**لقراءة سجل الأخطاء:** لوحة Vercel ← **Logs** (سجلات مباشرة).

---

## 💰 الحدود المجانية

| الخدمة | الحد المجاني |
|--------|--------------|
| Vercel | 100 جيجا نقل/شهر |
| Neon | 0.5 جيجا تخزين |
| Vercel Blob | 1 جيجا تخزين |

كافية تماماً لآلاف الإعلانات في البداية.

> ⚠️ قاعدة Neon المجانية تدخل **سبات** بعد 5 دقائق خمول، فيتأخر أول طلب 2–3 ثوانٍ. طبيعي ولا يؤثر على الاستخدام الفعلي.

---

## 🔒 بعد النشر مباشرة

1. **فعّل المصادقة الثنائية** لحساب المدير (مدعومة في المشروع).
2. **احتفظ بنسخة من `ENCRYPTION_KEY`** في مكان آمن — فقدانه يعني فقدان أرقام الهواتف المشفّرة.
3. **لا تشارك `SEED_SECRET`** ولا `ADMIN_PASSWORD` مع أحد.
4. **راجع سجل النشاط** من لوحة التحكم بين حين وآخر.
